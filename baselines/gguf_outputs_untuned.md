A pre-signed URL is a temporary URL that gives limited permission to upload or download a specific file from object storage

A collision happens when two different keys produce the same array index or hash bucket

In relational databases, ACID stands for Atomicity, Consistency, Isolation, and Durability, which are properties that guarantee database transactions are processed reliably.

Consistent hashing is a distributed routing algorithm that ensures that when a hash table is resized, only a minimum number of keys need to be remapped.

Transfer learning is a machine learning technique where a model developed for a task is reused as the starting point for a model on a second task.

Dynamic programming is an algorithmic technique for solving an optimization problem by breaking it down into simpler overlapping subproblems and storing their solutions.

Polymorphism in object-oriented programming is the provision of a single interface to entities of different types or the use of a single symbol to represent multiple different types.

Microservices architecture is an approach to developing a single application as a suite of small, independent services communicating over lightweight APIs.

CUDA is NVIDIA’s platform for using a GPU as a general-purpose parallel processor.

------------------------------------Answers---------------------------------------
{
"flashcards": [
{
"question": "What is a pre-signed URL used for in object storage?",
"answer": "It is a temporary URL that gives limited permission to upload or download a specific file."
},
{
"question": "What happens during a collision in hashing?",
"answer": "Two different keys produce the same array index or hash bucket."
},
{
"question": "What does ACID stand for in relational databases?",
"answer": "Atomicity, Consistency, Isolation, and Durability."
},
{
"question": "What is consistent hashing used for in distributed systems?",
"answer": "To ensure that when a hash table is resized, only a minimum number of keys need to be remapped."
},
{
"question": "What is transfer learning in machine learning?",
"answer": "A technique where a model developed for one task is reused as a starting point for another task."
},
{
"question": "What is dynamic programming?",
"answer": "An algorithmic technique that solves optimization problems by breaking them into overlapping subproblems and storing their solutions."
},
{
"question": "What is polymorphism in object-oriented programming?",
"answer": "The provision of a single interface to entities of different types or using a single symbol to represent multiple types."
},
{
"question": "What is microservices architecture?",
"answer": "An approach to developing a single application as a suite of small, independent services communicating over lightweight APIs."
},
{
"question": "What is CUDA?",
"answer": "NVIDIA’s platform for using a GPU as a general-purpose parallel processor."
}
]
}
