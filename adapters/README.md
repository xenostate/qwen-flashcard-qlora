# Adapters

Adapter weight files are kept locally and ignored by git because they are binary artifacts. The cleaned project keeps `flashcards-v1-best` as the local adapter bundle.

If you want to publish adapter weights with the repository, use Git LFS or upload the weights as a release artifact.
